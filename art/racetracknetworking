from tealight.net import connect, send
connect("racetracktest")
carNumber=0
x=0
users=[]
lastsend=[]
def handle_message(message):
  global carNumber, users
  if "type" in message:
    type = message['type']
    if type == "heartbeat":
       users[message['user_id']] = {"last-sent": message['payload']}
       print users
  if message== "connected":
    carNumber= carNumber + 1
    send ({"message_id": "registration", "data": carNumber})
  elif message== userId:
    users[x]=userID
    x+1
    print message
